import { define } from "../../utils.ts";

export default define.page(function TestPage() {
  return (
    <div class="body">
      <h1>Test Page</h1>
      <p>This is a test page.</p>
    </div>
  );
});
